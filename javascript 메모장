1. main .container span  효과
2. navigation target 효과
3. header 부분 계속 표시

document.querySelector 이걸로 main h2 span을 셀렉해라
                        그걸 spanEl에 대입해라

배열로 변경될 글씨를 넣어라

txtArr = [ , , ,]

출력할 때마다 어떤값인지 알아야하기때문에 
index  변수를 지정해주고 그건 0으로 함

출력됐다 업어지느건
메소드가 2개가 필요함

1. 
split. 하나하나 나누는 
Web Publisher 를 "W","e","b" 이렇게 나눠줘라

함수를 만들어줌
출력함수 지우는 함수
funtion 

currentTxt.shift() : 시프트 함수 >  전체 요소중에 맨 앞에 있는 요소를 없애고  spanEl에 없앤 요소를 붙여라 여기선 "W"
(파괴적 메소드)
currentTxt길이값이 0이 아니라면 setTimeout 해라
if 문요함수를 실행하되 Math.floor 만큰 참았다가 출력해라 여기선 0.1초

타이핑 효과 를 주기위해서 0.1초동안 랜덤으로 돌린다 

else 실행되면  spanEl이 가지고있는 텍스트 컨테츠 스플릿 한 값을 currentTxt에 대입
deleteTxt,를 3초 있다 실행해라

currentTxt.pop() 이건 맨뒤에 있는 거를 삭제하라는 메소드

currentTxt.join("") 위에 가지고는 있는 W e b 각각의 배열을 묶어준다 

if문 0이 아니라면 0.1초 랜덤값 이후에 pop 해라 :맨뒤에 있는 문자를 떼고 나머지 합치고 출력을 반복

index = (index +1)% txtArr.length
인덱스에 +1 을 해주고 txtArr.length 만큼 나누고 
나머지값을 
index에 저장하면 위의 출력함수를 반복 


맨밑의 라이터를 실해줘야하니 마지막에 트리거로서 실행문


즉시실행함수 (funtion)(){
   효과 코드
}


-----------------------------------------------


스크크롤을 내리면 header가 백그라운컬러가 검은색으로 계속 따라다닌 효과

css에 하나를 추가해야함

 header.active{
    background-color: rgba(0,0,0);
    animation: fadeIn 0.5s ease-in-out;
  }
  @keyframes feadIn{
    0%{
      opacity: 0;
    }
    100%{
      opacity: 1;
    }
  }
  이 요소를 추가해야함 
  백그라운드 블랙 에니메이션 0.5초마다 인앤아웃
  패드인 오파시티 0 1 

  해더쪽에서 액티브라는 녀석을 건드렸을 때


  html 코드 확인
   https://validator.w3.org/#validate_by_input

css 검사
jigsaw.w3.org